// ==========================================================================
//  VIGIL — Upgrade Banner (neutralized)
// ==========================================================================
//  Single-edition product — there are no upgrade paths to prompt for.
//  The former implementation rendered a "<feature> is available in Pro and
//  above" banner with an Upgrade button. Now it renders nothing. Kept as
//  a stub so the default export still resolves for any straggling imports.
// ==========================================================================

import React from 'react';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const UpgradeBanner = (_props: any) => null;

export default UpgradeBanner;
