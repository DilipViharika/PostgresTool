{
    "version": 2,
    "installCommand": "npm ci",
    "builds": [{ "src": "server.js", "use": "@vercel/node", "config": { "maxDuration": 30, "memory": 256 } }],
    "routes": [{ "src": "/(.*)", "dest": "server.js" }]
}
